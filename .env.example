# GitHub PAT with admin:org scope (for runner registration)
RUNNER_GH_TOKEN=

# Runner group in GitHub (optional, defaults to "Default")
RUNNER_GROUP=Default

# macOS ISO location on Proxmox storage
RUNNER_MACOS_ISO=nas:iso/macOS-Sequoia-15.iso
